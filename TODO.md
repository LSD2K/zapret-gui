# TODO

Свободный реестр того, что хочется сделать. Не план релиза — заметки и
идеи, чтобы было с чего начинать в следующих чатах. Сюда же слит бывший
`docs/NEXT-SESSION-ROADMAP.md` (крупные задачи того роадмапа выполнены —
см. `CHANGELOG.md`).

> **Соглашение.** Сделанное переносим в `CHANGELOG.md` и удаляем отсюда —
> здесь только **открытые** задачи и идеи. Контекст проекта, карта кода и
> архитектура — в `README.md` и `CoderManual.md` (не дублируем).
>
> Крупные реализованные вехи (единый слой маршрутизации, sing-box/mihomo
> с прокси-таблицами и режимом «эксперт», AmneziaWG, прозрачное
> проксирование + локальный режим для ПК с 1 NIC, пул серверов, тестер
> прокси, курируемые списки, выбор версии/транспорта при установке)
> описаны в README/CHANGELOG.

## MCP-сервер (управление через ИИ)

- [ ] **MCP-сервер `/api/mcp`** — управление всем функционалом GUI из
      MCP-клиента (Claude Desktop/Code, LM Studio, Cline): реестр
      инструментов с моделью разрешений, ресурсы-справочники, эксперименты
      со стратегиями nfqws2 с обратной связью (применил → измерил → откатил),
      shell-доступ ко всему роутеру (пакеты, службы, файлы) и самоправка
      модулей GUI на устройстве с авто-откатом. Готовый план и ТЗ —
      [`docs/mcp-server-plan.md`](docs/mcp-server-plan.md), порядок работ и
      бюджет контекста по сессиям —
      [`docs/mcp-implementation-roadmap.md`](docs/mcp-implementation-roadmap.md).
      **План нарезан на рабочие задания** (сессия S0): общий контракт —
      [`docs/mcp/00-contract.md`](docs/mcp/00-contract.md), по файлу на
      сессию `docs/mcp/NN-*.md`, эстафета —
      [`docs/mcp/HANDOFF.md`](docs/mcp/HANDOFF.md). Сам план целиком агенту
      не подаётся. **Сделано: S1** — транспорт, JSON-RPC, авторизация
      (`core/mcp/{schema,server,auth}.py`, `api/mcp.py`, секция `mcp` в
      настройках). **Сделано: S2** — реестр `@tool` с автозагрузкой
      `core/mcp/tools/*`, модель разрешений и граница записи настроек,
      маскировка секретов в одной точке, четыре read-only
      инструмента-эталона и скил [`mcp`](.claude/skills/mcp/SKILL.md).
      **Сделано: S3** — ресурсы `zapret://…` (живой `nfqws2 -?`, карта
      `--lua-desync`, каталоги, состояние), их зеркало инструментом
      `docs_get`, описания настроек `config_describe` и промты-сценарии
      (`core/mcp/{resources,config_docs,prompts}.py`,
      `core/mcp/tools/docs.py`). **Сделано: S4** — тринадцать read-only
      инструментов по nfqws2: стратегии и каталоги (`strategy_list`,
      `strategy_get`, `catalog_search`, `nfqws_command_preview`,
      `strategy_state_list`), списки и ассеты (`hostlists_list`,
      `hostlist_get`, `ipsets_list`, `lists_list`, `blobs_list`,
      `lua_functions_list`), правила перехвата (`firewall_status` с
      детектором расхождений) и «дошёл ли трафик до движка»
      (`traffic_recent` — лог nfqws2, DNS-детектор, conntrack); общая
      форма списка и пагинация с ужиманием под лимит ответа
      (`core/mcp/tools/_paging.py`). **Сделано: S5** — туннели и
      диагностика: `tunnels_status` (шесть движков одним ответом,
      сборка в `core/tunnels_overview.py`, `traffic_source` рядом с
      каждым числом), `diagnostics_run` (пассивная часть всегда,
      сетевые пробы — по разрешению `probes`, с бюджетом времени),
      `dpi_report` (последняя классификация DPI, проб не запускает) и
      `updates_check` (кеш по умолчанию, `refresh` — по `probes`).
      **Сделано: S6** — первая запись: `config_set` (дифф «было/стало»,
      список заменяется целиком, граница — в `permissions.is_writable`),
      `config_writable_paths`, журнал вызовов и снимки «до»
      (`core/mcp/audit.py`: `mcp-audit.jsonl` и `mcp-undo.json` рядом с
      `settings.json`), `audit_list` и `mcp_undo_last` — откат
      переживает перезагрузку роутера.
      **Сделано: S7** — управление движком и правка обхода:
      последовательность «firewall → движок → конфиг → автозапуск»
      вынесена в `core/nfqws_control.py` и стала общей для GUI, CLI и
      MCP; под `control` — `nfqws_start/stop/restart`, `strategy_apply`,
      `nfqws_reload_lists` (SIGHUP, не перезапуск),
      `firewall_apply`/`firewall_remove`; под `strategies_write` —
      `strategy_save`/`strategy_delete`, `hostlist_edit`/`ipset_edit`
      (`replace`/`add`/`remove`), `blob_add`, `lua_script_save`. Порты
      управления (SSH и веб-интерфейс) исключаются из NFQUEUE внутри
      `core/firewall.py`, то есть при любом применении правил. Откат
      расширен на семь видов снимков и переехал под «любое разрешение
      на запись». **Не вошло в S7 и ждёт отдельного PR:**
      `tunnel_up`/`tunnel_down` и остальные туннельные
      write-инструменты (`*_config_save`, `subscription_refresh`,
      `pool_refresh`, `unified_rule_*`) — подробности в
      [`docs/mcp/HANDOFF.md`](docs/mcp/HANDOFF.md).
      **Сделано: S8** — активные пробы и тяжёлые прогоны: `probe_targets`
      и `probe_compare` (вердикты `bypass_helps`/`no_difference`/
      `target_down`/`bypass_hurts`/`unknown`, `core/probe_runner.py`),
      подбор стратегий, blockcheck и blockcheck2, healthcheck и матрица
      связности; асинхронный контракт `job_id` → `*_status` →
      `*_output` (`core/mcp/tools/_jobs.py`), лимиты `mcp.probes`.
      **Сделано: S9** — общий мьютекс на движок
      (`core/nfqws_session.py`): захват с владельцем и временем
      (`acquire`/`holder`/`SessionBusy`), полный снимок состояния и
      идемпотентное восстановление, межпроцессный lock рядом с
      `settings.json` с кражей у мёртвого процесса. Мьютекс берут
      `core/nfqws_control` (start/stop/restart/apply_strategy),
      сканер на весь прогон и `probe_runner.compare` — подбор и
      применение стратегии больше не портят друг друга, а отказ
      называет держателя.
      **Сделано: S10 — ядро фичи.** Движок экспериментов
      `core/strategy_experiment.py`: варианты задаются тремя способами
      (`args` / `strategy_id` / `profiles`), baseline без обхода,
      медиана по повторам, `score` — формулой сканера (вынесена в
      `strategy_scanner.compose_score`, чтобы ранжирование совпадало с
      UI), дельта к baseline, хвост лога движка **по окну варианта** и
      правила-подсказки «почему не сработало» данными (`HINT_RULES`,
      дополняет S11). Состояние возвращается в `finally` на каждом
      шаге, `keep_best` оставляет победителя применённым только до
      `ttl_sec` (дедмен-свитч), а снимок лежит на диске рядом с
      `settings.json` — выключение питания посреди прогона тоже не
      оставляет временную стратегию: `recover_after_restart()` при
      старте GUI. Семь инструментов `strategy_experiment_*` под
      разрешением `experiments` (оно не действует без `control` и
      `probes`).
      **Сделано: S11** — сборка и валидация: `strategy_compose`
      (декларативные профили `filter`/`payload`/`desync` → argv **тем
      же** `build_nfqws_args`, что и UI; порядок сборки — по §15 скила;
      профили из ответа кладутся в `strategy_save` как есть),
      `strategy_validate` (`nfqws2 --intercept=0` по
      `strategy_id`/`args`/`profiles` — единственная проверка, ловящая
      ошибку ВНУТРИ lua) и линтер `core/strategy_lint.py` — восемь
      правил на то, чего валидация не ловит в принципе (неизвестная
      lua-функция, незаявленный blob, декларация после `--new`, порядок
      `--lua-init`, приём без фильтра). Ошибка линтера делает ответ
      ошибочным, но argv и команда в нём остаются. `HINT_RULES`
      дополнены тремя правилами из §16 скила. Цикл «собрал → проверил →
      сохранил → применил → измерил → откатил» замкнут.
      **Сделано: S12** — shell и система: `core/shell_exec.py` (два
      режима — argv без оболочки и `sh -c` под `shell_full`;
      safe-список командами-данными; запреты, которые не обходятся
      подтверждением, с нормализацией команды; двухшаговое
      подтверждение одноразовым токеном; дедмен-свитч `guard` с
      файлом на диске и перезарядкой при старте GUI) и пятнадцать
      инструментов поверх неё: `shell_exec`/`shell_exec_async` с
      фоновыми задачами, `file_read`/`file_list`/`file_write` (запись
      только внутрь `allow_write_paths`, атомарно, с откатом),
      `package_list/install/remove`, `service_list`/`service_control`,
      `system_reboot` под `dangerous`. `shell_full` открывает
      `shell_readonly` (`permissions.IMPLIES`), журнал пишет код
      возврата и первые строки вывода (`audit.note`).
      Точка выключена по умолчанию: токен пуст, все 11 разрешений
      `false`.
      **Сделано: S13** — самоправка модулей GUI на устройстве:
      `core/code_editor.py` (границы по резолвнутому пути, staging
      рядом с `settings.json` — правка не попадает на диск до
      `code_apply`, слепок дерева на симлинках для настоящего
      `import`, снимки с манифестом, написанным ДО правки файлов) и
      `core/code_guard.py` — **посторонний процесс только на stdlib**,
      который ждёт тела `/api/status` и возвращает прежние файлы, если
      GUI не вернулся или правку не подтвердили за `commit_ttl_sec`.
      Тринадцать инструментов `code_*` под `self_edit`; защищённое ядро
      (сторож, границы, авторизация, разрешения, реестр, конфиг) — под
      `self_edit_core`, и список этот в коде, а не только в настройках.
      `code_export_patch` выгружает локальные правки одним диффом, а
      `updates_check` предупреждает, что обновление GUI их затрёт.
      **Сделано: S14** — совместимость с живыми клиентами: legacy-SSE
      (`GET /api/mcp/sse` + `POST /api/mcp/messages` под флагом
      `mcp.transports.sse`, сессии и их уборка в `core/mcp/session.py`,
      разрыв клиента закрывает сессию, потолок `limits.max_sessions`),
      настоящая рассылка `notifications/tools/list_changed` при смене
      разрешений (опросом на круге keep-alive, а не крючком в трёх
      местах записи), stdio-мост `core/mcp/stdio.py` — «клиент пришёл по
      ssh» (`ssh router zapret-gui mcp --stdio`), локально или прокси в
      чужую точку, и подкоманда `zapret-gui mcp
      status|tools|call|token|audit|code` для отладки из консоли.
      Следующий шаг — **S15**
      ([`docs/mcp/15-ui.md`](docs/mcp/15-ui.md)): страница MCP в
      веб-интерфейсе; затем **S16** — README (раздела про MCP там нет
      вовсе), скил и приёмка на железе.
      Идея подсмотрена у [b4](https://docs.b4core.app/ru/docs/settings/mcp/).

## Полевое тестирование (приоритет)

Код написан, но не проверялся на реальном железе — нужны прогоны на
устройствах и репорты с `uname -a`:

- [ ] **Firewall TProxy / Redirect / Hybrid + DSCP** на Keenetic
      (KeenOS 4.x/5.x, iptables) и OpenWrt 22.03+ (nftables).
- [ ] **Локальный режим перехвата (`scope=self`)** на обычном Linux-ПК /
      VPS с одной сетевой картой — не рвёт ли входящие SSH/веб, не
      зацикливается ли DNS (профиль `pc`, `core/network_env.py`).
- [ ] **nftset-backend** IP-списков и **/tmp→флешка** на OpenWrt.
- [ ] **AmneziaWG-бинарники** под `mips-softfloat` на старых ядрах
      (musl syscall ABI).
- [ ] **Прозрачный режим + пул серверов** (urltest) под реальной
      нагрузкой LAN.
- [ ] **Миграция legacy AWG-правил → единый слой** на реальном конфиге с
      существующими `routing.rules` (идемпотентность `mig-*`, ничего не
      потеряно). После проверки — решить судьбу адаптера `awg_routing.js`
      / ссылок `#awg-routing` (можно убрать совсем).

## AmneziaWG / Configs

- [ ] **QR-код** для конфигов на странице Configs (генерация без
      зависимостей — рисовать PNG/SVG руками либо через встроенный awg).
- [ ] **Импорт `.conf` через QR с камеры** в браузере
      (`navigator.mediaDevices` + jsQR через CDN, опционально).

## Пул серверов / подписки (развитие)

- [ ] **Периодический health-re-test пула** — раз в N часов
      перетестировать `server-pool` и автоматически выкидывать стабильно
      мёртвые серверы из urltest-группы (сейчас фильтр живых только при
      сборке). Опереться на `core/proxy_tester.py`.
- [ ] **Diff при обновлении курируемого списка** — показывать в UI, что
      добавилось/удалилось при последнем refresh (хранится `_remote`
      снимок — данные уже есть).
- [ ] **geosite/geoip .db автообновление** — скачивать и обновлять по
      таймеру базы geosite/geoip для sing-box/mihomo (как курируемые
      списки), чтобы правила `geosite:` всегда были свежими.
- [ ] **Импорт подписки/сервера через QR** (тот же jsQR-путь, что для
      AWG).

## Каталоги стратегий

- [ ] **Решить судьбу источника каталогов.** `youtubediscord/zapret` снят
      в августе 2026 вместе со всей организацией (404), кнопка «Обновить
      стратегии» и загрузчик убраны — каталоги теперь только из сборки
      GUI. Нужно решение: (а) оставить как есть и обновлять каталоги
      вместе с релизами, (б) выбрать форк/зеркало и вернуть загрузчик из
      истории git. Второе — не механическая правка `SOURCE_OWNER`: это
      каталог desync-стратегий, то есть доверие чужому коду, поэтому
      источник нужно выбирать осознанно и вернуть запись в
      `docs/upstream.json` (с `paths` и `content_checks`), чтобы сторож
      апстримов снова следил за форматом.

## Диагностика / UX

- [ ] **«Почему домен не открывается» → авто-подсказка метода** —
      связать вывод BlockCheck (`remediation`: zapret/tunnel/dns) с
      кнопкой «создать маршрут» нужного типа прямо из результатов
      (вход уже есть: `core/testers` + единый слой; в blockcheck.js
      remediation пока только отображается чипом).
- [ ] **«Курируемый список → nfqws2-hostlist» напрямую** — кнопка на
      странице «Списки», помимо «→ Маршрут» (для тех, кто не пользуется
      единым слоем; сейчас nfqws2 доступен только как метод маршрута).
- [ ] **Профили «режимов»** на главной — один клик переключает набор
      активных туннелей и routing-правил («Дом» / «В дороге» /
      «Стриминг»). Реализовать как пресеты поверх единого слоя.
- [ ] **Уведомления** (Telegram/webhook) при падении туннеля, смене
      сервера в urltest, провале автообновления подписки/списка.

## Наблюдаемость

- [ ] **Метрики Prometheus** на `/metrics` — AWG handshake age,
      нормированный RX/TX, число активных routing-правил, статус пула.
      Полезно тем, у кого Grafana.

## AWG / watchdog (follow-up из роадмапа)

- [ ] **Watchdog AWG: ускорить логику/дефолты.** Владелец отмечал, что
      «не помогает / срабатывает долго». Дефолты медленные (handshake
      180 с, cooldown 300 с). После сбора данных из «Сохранённого лога» —
      решить, ускорять ли (есть `tests/test_awg_watchdog.py` — не
      сломать) и помогает ли рестарт вообще.
- [ ] **Лимит памяти amneziawg-go ВКЛ по умолчанию на роутерах** — если
      по логам подтвердится OOM (сейчас GOGC/GOMEMLIMIT ВЫКЛ по
      умолчанию, карточка в AWG→Дашборд).

## Технический долг

- [ ] **i18n** — UI русскоязычный. Выделить строки в словарь
      (`web/js/i18n/{ru,en}.js`) и переключатель языка.
- [ ] **Расширить покрытие тестами** — без юнит-тестов осталось:
      `strategy_scanner`, `blockcheck` (Python-оркестратор проб),
      `diagnostics` (сетевые части ping/http/dns), `warp_generator`.
