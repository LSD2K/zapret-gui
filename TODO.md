# TODO

Свободный реестр того, что хочется сделать. Не план релиза — заметки и
идеи, чтобы было с чего начинать в следующих чатах. Сюда же слит бывший
`docs/NEXT-SESSION-ROADMAP.md` (крупные задачи того роадмапа выполнены —
см. `CHANGELOG.md`).

> **Соглашение.** Сделанное переносим в `CHANGELOG.md` и удаляем отсюда —
> здесь только **открытые** задачи и идеи. Контекст проекта, карта кода и
> архитектура — в `README.md` и `CoderManual.md` (не дублируем).
>
> Крупные реализованные вехи (единый слой маршрутизации, sing-box/mihomo
> с прокси-таблицами и режимом «эксперт», AmneziaWG, прозрачное
> проксирование + локальный режим для ПК с 1 NIC, пул серверов, тестер
> прокси, курируемые списки, выбор версии/транспорта при установке)
> описаны в README/CHANGELOG.

## MCP-сервер: следующий круг

Сам сервер сделан и описан (README → «Управление через ИИ (MCP)», скил
[`mcp`](.claude/skills/mcp/SKILL.md), архив рабочих заданий —
[`docs/mcp/README.md`](docs/mcp/README.md)). Здесь — то, что сознательно
не вошло в первый заход (§14 плана) и долги, оставшиеся по дороге.

**Долги:**

- [ ] **`tunnels_write` — разрешение без инструментов.** Переключатель
      есть с S2, а туннельных write-инструментов нет: `tunnel_up` /
      `tunnel_down`, `*_config_save`, `subscription_refresh`,
      `pool_refresh`, `unified_rule_*`. Не вошло в S7; пока модель
      умеет читать `tunnels_status`, но не поднять туннель. Включённый
      сегодня переключатель не даёт ничего — и это сказано в README,
      но лечится не документацией.
- [ ] **`mcp.transports.http` ни на что не влияет** (долг S14):
      переключателя на странице нет намеренно, но флаг в настройках
      висит. Либо научить его выключать основной транспорт, либо убрать.
- [ ] **Приёмка на реальном роутере** — см. блок ниже: пункты 2, 3 и 6
      в CI не воспроизводятся в принципе.
- [ ] **`lua_script_get` — писать можно, читать нечем.** Под
      `strategies_write` модель сохраняет lua-скрипт целиком
      (`lua_script_save`), но прочитать существующий не может: карту
      функций отдаёт `lua_functions_list`, а исходный текст — только
      `file_read`, то есть под `shell_readonly`. Правка «поменяй в этом
      скрипте одну строку» требует разрешения на чтение всей файловой
      системы — несоразмерно. Нужен `lua_script_get` (и, вероятно,
      `lua_script_delete` + точечный патч по образцу `code_patch`) под
      тем же `strategies_write`, что и запись.

**Идеи следующего круга:**

- [ ] **Встроенный агент в GUI** — клиент к OpenAI-совместимому API
      (LM Studio / Ollama) прямо на странице: «кнопка: подбери
      стратегию сам», с теми же 93 инструментами внутри. Вся логика —
      поверх готового реестра, отдельная страница и отдельный флаг.
- [ ] **Обучение на истории экспериментов** — складывать результаты
      `strategy_experiment_*` в локальную базу (домен → что сработало у
      этого провайдера) и отдавать её ресурсом `zapret://…`: модель
      начинает с того, что уже работало, а не с нуля.
- [ ] **`resources/subscribe` + `notifications/resources/updated`** —
      живой статус вместо опроса. Канал для уведомлений уже есть
      (legacy-SSE, S14).
- [ ] **Авто-PR из роутера** — `code_export_patch` + отправка патча в
      GitHub: удачная правка с устройства сразу становится pull
      request'ом, а не теряется при обновлении GUI.
- [ ] **Терминал в GUI поверх `core/shell_exec.py`** — async-задачи и
      SSE уже готовы. Удобно и человеку, и для разбора того, что
      наделала модель.
- [ ] **Экспорт удачной стратегии в формат каталога** `catalogs/*.txt`
      — чтобы найденное на устройстве уходило PR-ом в сообщество.
- [ ] **`traffic_capture` — снифер как инструмент, а не как shell.**
      Сейчас посмотреть, что реально ушло в сеть ПОСЛЕ nfqws2, можно
      только запустив `tcpdump` через `shell_exec_async`, то есть выдав
      `shell_full` (root на роутере). Для обратной связи «вижу, как
      изменился трафик» это несоразмерная цена. Нужен инструмент под
      `probes`: фиксированный argv `tcpdump -i <wan> -nn -c N -s N
      <фильтр>`, потолки на число пакетов и время, разбор в поля
      (направление, флаги, TTL, длина, SNI) вместо сырого дампа,
      автоудаление файла. Внутри эксперимента — снимать по окну
      варианта, рядом с хвостом лога движка. Альтернатива или
      дополнение: `--writable` + `zapret-pcap.lua` (движок пишет pcap
      сам, до и после десинка) — нашей сборкой аргументов `--writable`
      сейчас не выдаётся вовсе.

### Приёмка MCP на живом роутере (§17 плана)

Не «прогнать тесты», а проверить руками, с секундомером. Репорт — с
`uname -a` и моделью устройства:

- [ ] **Не подвисает.** Во время скана, эксперимента и
      `shell_exec_async` веб-интерфейс остаётся отзывчивым.
- [ ] **Не рвёт управление.** После намеренно вредной
      последовательности (гасим интерфейс, флашим firewall, ломаем
      модуль) GUI и SSH остаются доступны **сами**, без вмешательства
      человека. Это главный приёмочный тест.
- [ ] **Переживает перезапуск.** Токен, разрешения, журнал, снимки кода
      и незакоммиченные правки читаются после `reboot`; незавершённый
      эксперимент не оставляет применённой временную стратегию.
- [ ] **Не течёт.** Секреты не появляются ни в одном ответе; лог не
      забивается при переборе токена; временные файлы чистятся.
- [ ] **Влезает в роутер (128 МБ RAM).** Ответы ≤ лимита, тяжёлая
      операция одновременно одна, память MCP-слоя не растёт от числа
      вызовов — проверить сутки под периодическим опросом.
- [ ] **Работает у живых клиентов.** Claude Code, Claude Desktop,
      LM Studio и хотя бы один OpenAI-совместимый мост: список
      инструментов виден, вызовы проходят, длинные операции не
      отваливаются по таймауту клиента.
- [ ] **Сценарии Приложения D проходят целиком**, без ручных подсказок
      между шагами: «почему не открывается rutracker», «подбери
      стратегию для YouTube», «сколько свободно места», «поставь htop»,
      «перезапусти dnsmasq», «покажи системный лог», «перезагрузи
      роутер», «что ты вообще можешь».

## Полевое тестирование (приоритет)

Код написан, но не проверялся на реальном железе — нужны прогоны на
устройствах и репорты с `uname -a`:

- [ ] **Firewall TProxy / Redirect / Hybrid + DSCP** на Keenetic
      (KeenOS 4.x/5.x, iptables) и OpenWrt 22.03+ (nftables).
- [ ] **Локальный режим перехвата (`scope=self`)** на обычном Linux-ПК /
      VPS с одной сетевой картой — не рвёт ли входящие SSH/веб, не
      зацикливается ли DNS (профиль `pc`, `core/network_env.py`).
- [ ] **nftset-backend** IP-списков и **/tmp→флешка** на OpenWrt.
- [ ] **AmneziaWG-бинарники** под `mips-softfloat` на старых ядрах
      (musl syscall ABI).
- [ ] **Прозрачный режим + пул серверов** (urltest) под реальной
      нагрузкой LAN.
- [ ] **Миграция legacy AWG-правил → единый слой** на реальном конфиге с
      существующими `routing.rules` (идемпотентность `mig-*`, ничего не
      потеряно). После проверки — решить судьбу адаптера `awg_routing.js`
      / ссылок `#awg-routing` (можно убрать совсем).

## AmneziaWG / Configs

- [ ] **QR-код** для конфигов на странице Configs (генерация без
      зависимостей — рисовать PNG/SVG руками либо через встроенный awg).
- [ ] **Импорт `.conf` через QR с камеры** в браузере
      (`navigator.mediaDevices` + jsQR через CDN, опционально).

## Пул серверов / подписки (развитие)

- [ ] **Периодический health-re-test пула** — раз в N часов
      перетестировать `server-pool` и автоматически выкидывать стабильно
      мёртвые серверы из urltest-группы (сейчас фильтр живых только при
      сборке). Опереться на `core/proxy_tester.py`.
- [ ] **Diff при обновлении курируемого списка** — показывать в UI, что
      добавилось/удалилось при последнем refresh (хранится `_remote`
      снимок — данные уже есть).
- [ ] **geosite/geoip .db автообновление** — скачивать и обновлять по
      таймеру базы geosite/geoip для sing-box/mihomo (как курируемые
      списки), чтобы правила `geosite:` всегда были свежими.
- [ ] **Импорт подписки/сервера через QR** (тот же jsQR-путь, что для
      AWG).

## Каталоги стратегий

- [ ] **Решить судьбу источника каталогов.** `youtubediscord/zapret` снят
      в августе 2026 вместе со всей организацией (404), кнопка «Обновить
      стратегии» и загрузчик убраны — каталоги теперь только из сборки
      GUI. Нужно решение: (а) оставить как есть и обновлять каталоги
      вместе с релизами, (б) выбрать форк/зеркало и вернуть загрузчик из
      истории git. Второе — не механическая правка `SOURCE_OWNER`: это
      каталог desync-стратегий, то есть доверие чужому коду, поэтому
      источник нужно выбирать осознанно и вернуть запись в
      `docs/upstream.json` (с `paths` и `content_checks`), чтобы сторож
      апстримов снова следил за форматом.

## Диагностика / UX

- [ ] **«Почему домен не открывается» → авто-подсказка метода** —
      связать вывод BlockCheck (`remediation`: zapret/tunnel/dns) с
      кнопкой «создать маршрут» нужного типа прямо из результатов
      (вход уже есть: `core/testers` + единый слой; в blockcheck.js
      remediation пока только отображается чипом).
- [ ] **«Курируемый список → nfqws2-hostlist» напрямую** — кнопка на
      странице «Списки», помимо «→ Маршрут» (для тех, кто не пользуется
      единым слоем; сейчас nfqws2 доступен только как метод маршрута).
- [ ] **Профили «режимов»** на главной — один клик переключает набор
      активных туннелей и routing-правил («Дом» / «В дороге» /
      «Стриминг»). Реализовать как пресеты поверх единого слоя.
- [ ] **Уведомления** (Telegram/webhook) при падении туннеля, смене
      сервера в urltest, провале автообновления подписки/списка.

## Наблюдаемость

- [ ] **Метрики Prometheus** на `/metrics` — AWG handshake age,
      нормированный RX/TX, число активных routing-правил, статус пула.
      Полезно тем, у кого Grafana.

## AWG / watchdog (follow-up из роадмапа)

- [ ] **Watchdog AWG: ускорить логику/дефолты.** Владелец отмечал, что
      «не помогает / срабатывает долго». Дефолты медленные (handshake
      180 с, cooldown 300 с). После сбора данных из «Сохранённого лога» —
      решить, ускорять ли (есть `tests/test_awg_watchdog.py` — не
      сломать) и помогает ли рестарт вообще.
- [ ] **Лимит памяти amneziawg-go ВКЛ по умолчанию на роутерах** — если
      по логам подтвердится OOM (сейчас GOGC/GOMEMLIMIT ВЫКЛ по
      умолчанию, карточка в AWG→Дашборд).

## Технический долг

- [ ] **i18n** — UI русскоязычный. Выделить строки в словарь
      (`web/js/i18n/{ru,en}.js`) и переключатель языка.
- [ ] **Расширить покрытие тестами** — без юнит-тестов осталось:
      `strategy_scanner`, `blockcheck` (Python-оркестратор проб),
      `diagnostics` (сетевые части ping/http/dns), `warp_generator`.
