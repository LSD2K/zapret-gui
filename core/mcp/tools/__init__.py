# core/mcp/tools/__init__.py
"""
Инструменты MCP: по модулю на домен.

Модули этого пакета находит и импортирует ``core/mcp/registry.py``
(``pkgutil``) при первом обращении к реестру — **перечислять их нигде
не надо**: положили файл, объявили в нём ``@tool`` — инструмент в
``tools/list``.

Правила, общие для всех модулей пакета:

* **Тонкая обёртка.** Логика живёт в менеджерах (``core/*.py``), сюда
  попадает только разбор аргументов и компактная упаковка ответа. Не
  хватает функции — она добавляется в менеджер и становится доступна
  заодно UI и CLI, а не пишется здесь.
* **Импорт менеджеров — внутри обработчика**, а не на уровне модуля:
  реестр грузится при первом запросе к MCP, и тяжёлые импорты (firewall,
  сканер) на роутере это заметно. Заодно инструмент, чей менеджер на
  этом устройстве не поднимается, не ломает весь реестр.
* **Ответ компактный и одинаковый по форме.** Успех и неуспех несут
  одни и те же поля: ``ok``, ``error``/``hint`` при неудаче,
  ``items``/``count``/``truncated`` у списков. Модель читает ответы
  десятками — разнобой в именах полей она тратит на догадки.
* **«Не знаю» вместо выдумки.** Нет данных — так и пишем и показываем,
  что есть рядом (``available``, ``sources``).

Эталон формы — ``status.py`` (простейший), ``config.py`` (путь + глубина)
и ``logs.py`` (фильтры + лимит + пагинация). Новые инструменты делаются
по ним.
"""
